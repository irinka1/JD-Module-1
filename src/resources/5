/*5. Найти клиента (customer), которая приносит меньше всего прибыли компании (company) для каждой из компаний .*/

SELECT  CUSTOMER_ID, cost
FROM project
  /*WHERE project.COMPANY_ID = 3*/
ORDER BY cost limit 1;