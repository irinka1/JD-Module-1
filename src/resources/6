/*Вычислить, среднюю ЗП программистов в проекте, который приносит наименьшую прибыль.*/

SELECT sum(devSalary) / count(devName) as MediumSalary
FROM (
       SELECT developers.salary as devSalary, developers.NAME as devName, cost
       FROM developers, project
       WHERE developers.company_id=project.COMPANY_ID
             AND project.cost=(SELECT MIN(project.cost) FROM project)) AS table1